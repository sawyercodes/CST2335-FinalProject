<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.victo.cst2335_finalproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/ThemeOverlay.AppCompat.Dark">

        <activity
            android:name=".HouseSettings"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--<activity android:name=".GarageFragment" />-->
        <activity
            android:name=".HouseSettingsFragments"
            android:label="@string/title_activity_house_settings_container"></activity>
    </application>

</manifest>